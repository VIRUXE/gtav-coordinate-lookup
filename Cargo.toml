[package]
name = "gtav-coordinate-lookup"
version = "1.0.0"
edition = "2021"
license = "Unlicense"

[profile.release]
strip = true
lto = true
codegen-units = 1

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
zip = { version = "8.6.0", default-features = false, features = ["deflate"] }
